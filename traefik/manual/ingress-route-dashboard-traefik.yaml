apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard-secure
spec:
  entryPoints:
  - web
  routes:
  - kind: Rule
    match: Host("dashboard.lair.pangarabbit.com") && PathPrefix("/api")
    services:
    - name: dashboard
      port: 9000
#    middlewares:
#    - name: dashboard-auth
#    - name: dashboard-rewrite
  - kind: Rule
    match: Host("dashboard.lair.pangarabbit.com") && PathPrefix("/api")
    services:
    - name: dashboard
      port: 9000
#    middlewares:
#    - name: dashboard-auth